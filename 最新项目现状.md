# SafeFlow 最新项目现状

## 1. 项目一句话概述

SafeFlow 当前已经是一个**可运行的内容审核后端系统**，核心链路覆盖：

- 文本审核：规则引擎优先 + LLM 智能审核兜底
- 人工复审：review 入队、人工领取、人工裁决、结果回传
- 案例沉淀：人工裁决结果可写入案例库
- 策略治理：规则 CRUD、规则生成、规则回测、Prompt 版本化
- 运维能力：Prometheus、Grafana、Jaeger、健康检查
- 多模态方向：已具备独立图片审核服务

项目目前最像一个“后端审核平台雏形”，而不是单一 demo。

---

## 2. 当前整体架构

当前代码结构对应的主要服务如下：

1. `api-gateway`
   - 对外 HTTP 入口
   - 负责文本审核、批量审核、结果查询、管理后台接口

2. `rule-engine`
   - 基于关键词、正则、AC 自动机的快速规则匹配

3. `llm-agent`
   - 基于 Ark + Eino 的智能审核服务
   - 支持 Pipeline 模式与旧版 Eino Agent 兼容降级

4. `image-moderation`
   - 独立的图片审核服务
   - 支持 HTTP 与 RPC 两种入口

5. `audit-service`
   - 订阅审核结果事件并落库

6. `init-milvus`
   - 初始化向量库与案例 embedding 数据

---

## 3. 图片识别 / 图片审核当前状态

### 3.1 结论

**理论上已经“基本做完”，但从工程完整度看属于“已打通主链路、尚未完全收口”的状态。**

换句话说，不是“完全没做”，而是：

- 图片审核服务本身已经独立存在
- 图像风险识别 + OCR 客户端已经有真实实现
- HTTP 与 RPC 入口都已经接通
- 但仍有一些明显的工程化遗留点，说明它还不是最终稳定版

### 3.2 已完成的部分

#### 已有独立图片审核服务

- `cmd/image-moderation/main.go` 会同时启动：
  - HTTP 服务
  - Kitex RPC 服务
- 并注册 `/moderate/image` 与 `/health`

说明图片审核不是草稿接口，而是一个单独服务。

#### 已有真实图像审核客户端

`internal/imagemod/volcengine_client.go` 已实现：

- 火山引擎图像审核请求签名
- 调用 `ImageContentRisk`
- 返回标签、置信度、建议动作

这说明视觉内容审核并不是 mock。

#### 已有 OCR 客户端

`internal/imagemod/ocr_client.go` 已实现：

- OCR 请求封装
- 调用火山 OCR 接口
- 提取全文、平均置信度、文本块信息

这说明图中文字识别链路也不是占位函数。

#### 已在服务层汇总结果

`internal/imagemod/service.go` 中的 `Moderate` 已完成：

- 调用图像审核
- 调用 OCR
- 组装最终 `ImageModerationResponse`
- 返回 `action / risk_score / reason / labels / text_content / ocr_result`

因此，从“服务实现”角度，图片审核主链路已经存在。

### 3.3 当前不足和注意点

#### 仍依赖火山引擎密钥

当前图片审核是否真正可用，强依赖：

- `VOLCENGINE_ACCESS_KEY`
- `VOLCENGINE_SECRET_KEY`
- `VOLCENGINE_REGION`

如果没配，图像审核和 OCR 都会失败，服务会回落到 `review`。

#### 当前 HTTP 接口只接收 `image_url`

`internal/imagemod/service.go` 的 HTTP handler 目前只接收：

- `image_url`
- `scene`

没有把图片二进制上传作为主入口暴露出来，所以当前更适合“传 URL 测试”，不适合直接传文件。

#### OCR 被强制开启

服务代码里存在这段测试性逻辑：

- 收到请求后，直接把 `EnableOcr` 改成 `true`

这说明功能是通的，但实现还带有测试痕迹，后续应该改为尊重请求参数。

#### 网关层没有统一代理图片审核

当前 `api-gateway` 主要持有：

- 规则引擎 client
- LLM agent client

并没有看到它把图片审核服务统一代理成文本审核那样的一站式入口。所以图片审核目前更像**独立服务能力**，而不是完全并入统一审核网关。

#### 仓库里保留了旧版占位实现

`internal/multimodal/` 下仍保留旧的占位式图片审核实现，其中有明显的 TODO / mock 逻辑。  
这部分更像历史实验代码，当前真正应该看的实现是 `internal/imagemod/`。

### 3.4 对图片能力的最终判断

当前图片能力可判断为：

- **功能层面：已实现**
- **服务层面：已打通**
- **联调层面：可测试**
- **产品层面：还差一点收尾**

如果你问“这个项目是不是已经搞定图片识别了”，更准确的说法是：

> **已经具备真实图片审核和 OCR 能力，但还不是完全打磨完毕的生产版。**

---

## 4. 文本审核链路现状

### 4.1 规则引擎

文本审核第一层已经可用：

- 关键词匹配
- 正则匹配
- AC 自动机优化
- 内置 PII 识别（如邮箱、手机号）

这部分适合做高确定性拦截。

### 4.2 LLM 审核

LLM 审核部分当前为“双实现并存”：

1. 新版 Pipeline
   - Classify
   - RAG
   - Decide
   - Validate

2. 旧版 Eino Agent
   - 作为兼容降级路径保留

当前实际逻辑是：

- 优先用 Pipeline
- Pipeline 失败则降级到旧版 Eino Agent

### 4.3 Milvus / RAG

Milvus 初始化链路已修复并验证通过：

- 配置读取问题已修复
- Ark Endpoint 已接入代码
- 向量初始化命令可正常执行

所以当前 RAG 能力已经从“概念存在”提升到“可初始化、可运行”。

---

## 5. 人工复审与闭环能力现状

相较于项目早期状态，这部分已经明显推进。

### 5.1 已完成

- review 任务入队
- 复审任务列表查询
- 复审任务领取
- 复审任务裁决
- 裁决结果写回 `ModerationResult`
- 人工裁决结果写入案例库
- 最终结果支持回调业务方
- 回调任务支持记录与重试

这意味着“人工复审 → 结果回传 → 案例沉淀”的最小闭环已经具备。

### 5.2 当前形态

它已经不是“只有一个 review 状态字段”，而是真正具备以下对象：

- `ReviewTask`
- `ModerationResult`
- `CallbackTask`

也就是说，这部分已经从想法层走到代码落地层。

---

## 6. 管理后台与治理能力现状

当前管理端已具备：

- 规则管理：增删改查
- 案例管理：列表 / 新增
- 审计日志：分页、多条件筛选
- Prompt 策略：创建、查询、激活
- 规则生成：基于案例生成候选规则
- 规则回测：按历史案例计算 Precision / Recall
- 复审统计：待处理 / 处理中 / 已完成
- 回调任务查看与手动重试
- 模拟流量启动 / 停止 / 状态查看

从后端接口丰富度来说，项目已经明显超过“单接口 demo”。

---

## 7. 当前完成度评估

### 7.1 可以认为已经完成的模块

#### 已基本完成

- 文本审核主链路
- 规则引擎
- LLM 审核主链路
- Milvus 初始化与 RAG 基础接入
- 人工复审基础闭环
- 结果查询与回调任务机制
- 管理后台大部分核心接口
- 可观测性基础设施

### 7.2 已实现但仍需继续打磨的模块

#### 已打通但未完全收口

- 图片审核统一接入
- OCR 开关控制
- 模型配置管理一致性
- 回调任务失败信息与响应码记录细节
- 规则回测的真实生产化评估流程

### 7.3 仍偏“后续增强项”的模块

#### 还不算完成

- RBAC / 多租户
- 更完整的人工审核工作台前端
- 更成熟的业务回传协议
- 更稳定的模型限流/熔断/降级治理
- 更完善的图片二进制上传审核入口

---

## 8. 当前主要风险

### 8.1 外部依赖风险

- Ark 模型配额、限流、Safe Experience Mode 会直接影响 LLM 审核
- 火山引擎图像审核/OCR依赖 AccessKey/SecretKey
- Milvus、MySQL、NATS 都必须先启动

### 8.2 配置风险

- `.env` 与 `config.yaml` 并存时，环境变量会覆盖配置文件
- 模型切换时，容易误以为改了 `config.yaml` 就生效，实际上可能被 `.env` 覆盖

### 8.3 工程一致性风险

- `internal/imagemod/` 与 `internal/multimodal/` 存在两套图片相关实现
- 根目录 `handler.go` 仍是生成后的 TODO 骨架文件，容易造成理解干扰

---

## 9. 建议的下一步优先级

### P0

1. 收口图片审核入口，只保留一套正式实现
2. 去掉 OCR 强制开启的测试逻辑
3. 统一模型配置来源，避免 `.env` 与 `config.yaml` 互相覆盖造成误判

### P1

1. 在网关层统一暴露图片审核入口
2. 补图片审核的真实联调测试用例
3. 补回调失败详情、响应码、重试状态的管理端展示

### P2

1. 增加人工审核运营界面的前端
2. 做更完整的策略评估与质量指标看板
3. 清理历史实验代码与占位实现

---

## 10. 最终结论

截至当前代码状态，SafeFlow 可以判断为：

> **一个已经具备可运行审核闭环的后端项目，文本审核主链路已基本成型，人工复审与策略治理已有较完整落地，图片审核能力已经接通但仍需收尾和统一。**

如果只问一句：

> **图片识别理论上已经做到了“能跑、能测、能返回结果”，但还没有完全打磨成最终生产态。**
