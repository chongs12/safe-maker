# SafeFlow 项目状态与路线图

## 1. 项目概览
SafeFlow 是一个基于 **CloudWeGo** 生态构建的高性能多模态内容审核平台。它利用微服务架构提供可扩展的文本和图像审核能力，结合了确定性的基于规则的引擎和先进的基于大语言模型（LLM）的智能体。

### 核心技术栈
- **框架**: Kitex (RPC), Eino (LLM 智能体编排), Gin (HTTP 网关)。
- **AI/ML**: 火山引擎 (图像审核/OCR), 豆包 (通过 Ark 接入 LLM), Milvus (用于 RAG 的向量数据库)。
- **基础设施**: Docker Compose, NATS (消息队列), MySQL (持久化)。
- **可观测性**: Prometheus, Grafana, Jaeger (OpenTelemetry)。

## 2. 已实现功能

### 2.1 多模态审核
- **文本审核**:
  - **规则引擎**: 使用 Aho-Corasick 算法进行高性能关键词匹配，用于即时的“允许/阻止”决策。
  - **LLM 智能体**: 针对复杂情况（“人工审核”）的上下文感知审核，能够理解语义细微差别和意图。
- **图像审核**:
  - **视觉分析**: 集成火山引擎，用于场景检测（UGC、广告、个人资料）和风险评分。
  - **OCR 能力**: 从图像中提取文本，以检测嵌入的文本违规内容。

### 2.2 先进的 LLM 智能体流水线
基于 `Eino` 构建，LLM 智能体遵循精细的 4 阶段流水线：
1.  **分类节点 (Classify Node)**: 确定内容类别以及是否需要 RAG 检索。
2.  **RAG 节点 (RAG Node)**: 如果需要，从 Milvus 向量数据库中检索相关的上下文或规则。
3.  **决策节点 (Decide Node)**: 根据上下文做出核心审核决策（允许/阻止/人工审核）。
4.  **验证节点 (Validate Node)**: 自我反思步骤，验证决策的一致性和置信度。

### 2.3 服务架构
- **IDL 优先设计**: 所有服务均通过 Thrift 定义（`safeflow.thrift`）。
- **可观测性**: 所有服务内置指标监控和链路追踪。
- **服务发现**: （在当前开发设置中通过 Kitex/Etcd 或直接寻址实现）。

## 3. 未来改进与路线图

### 3.1 近期计划（短期）
- [ ] **审计服务 (Audit Service)**: 全面实现 `audit-service`，以记录所有审核操作和结果，用于合规性和分析。
- [ ] **API 网关 (API Gateway)**: 完成 API 网关，将外部 HTTP 请求路由到内部 RPC 服务，并具备完善的身份验证和限流功能。
- [ ] **测试**: 提高单元测试覆盖率（目前专注于 `test-multimodal`），并为整个流水线添加集成测试。

### 3.2 功能增强（中期）
- [ ] **动态规则管理**: 允许在不重启服务的情况下运行时更新规则引擎（添加/删除关键词）。
- [ ] **自定义模型微调**: 在 LLM 智能体中支持针对特定领域（如游戏、电子商务）的微调模型。
- [ ] **视频审核**: 扩展 `image-moderation` 服务，支持视频帧提取和音频转录分析。

### 3.3 基础设施与运维（长期）
- [ ] **K8s 部署**: 为 Kubernetes 部署创建 Helm charts。
- [ ] **多租户支持**: 实现严格的数据隔离和配置管理，以支持多个租户/客户。
- [ ] **管理后台 UI**: 一个用于查看审核日志和配置规则的前端管理控制台。
